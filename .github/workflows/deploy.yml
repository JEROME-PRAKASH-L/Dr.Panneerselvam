name: Validate portfolio

# The site is published via GitHub Pages "Deploy from a branch" (Settings ->
# Pages -> Source: Deploy from a branch -> main / root), which serves the
# static files at the repo root directly. No Actions-based deploy is needed,
# so this workflow only validates the site on pushes and PRs.

on:
  push:
    branches:
      - main
      - claude/portfolio-b-corporate-Ln6Qj
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate static site
        run: |
          test -f index.html
          test -f data.js
          test -f assets/portrait.webp
          node --check data.js
          echo "Static site validated."
